{
  "name": "life-os",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "build:mobile": "cd mobile && eas build -p android --profile preview"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
